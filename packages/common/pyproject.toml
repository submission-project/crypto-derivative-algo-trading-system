[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "prometheus-client>=0.25.0",
    "pydantic-settings>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
