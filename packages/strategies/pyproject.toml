[project]
name = "strategies"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "common",
    "schemas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
common = { workspace = true }
schemas = { workspace = true }
